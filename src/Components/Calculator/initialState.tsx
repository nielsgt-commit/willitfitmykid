import type { CalculatorState } from "../types";

export const initialState: CalculatorState = {
    age: { months: 20 },
    height: { cm: 170 },
    percentile: { value: 50 },
    size: { label: 86 },
}